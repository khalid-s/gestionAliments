{% extends 'base.html.twig' %}

{% block title %} Modification {% endblock %}
{% block monTitre %} Modification : {{type.libelle}} {% endblock %}

{% block body %}
<h1 class="text-center border border-dark bg-dark rounded m-2 p-2 text-white">
    {% if modification %}
        Modification de : {{type.libelle}}
    {% else %}
        Ajout d'un Type        
    {% endif %}
</h1>
    {{form_start(form)}}
        <a href="{{path("admin_types")}}">Retour</a>
        <input type="submit" class="btn btn-primary" value="Enregistrer">
    {{form_end(form)}}
{% endblock %}
